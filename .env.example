COMPOSE_PROJECT_NAME=vpn_admin_panel

APP_PROVIDER=xray
DATABASE_URL=sqlite:///./data/data.db
API_TOKEN=admin-token

# dashboard auth
ADMIN_USERNAME=admin
# generate hash: python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt']).hash('change-me'))"
ADMIN_PASSWORD_HASH=
# fallback for local bootstrap only
ADMIN_PASSWORD=admin123
AUTH_SESSION_TTL_HOURS=24

# runtime profile defaults (deploy.sh rewrites these by mode)
APP_ENV=production
DEV_ROLE_EMULATION=false
SCHEMA_MANAGEMENT_MODE=alembic

# ingress and compatibility settings
DOMAIN=
ENABLE_TLS_PROXY=false
ENABLE_XRAY_PUBLIC=false
XRAY_PUBLIC_PORT=8443

# frontend build vars
VITE_API_URL=
VITE_API_TOKEN=
