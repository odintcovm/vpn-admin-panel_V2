services:
  avg:
    image: alpine:3.20
    command: ["sh", "-c", "while true; do echo '[avg] runtime alive'; sleep 30; done"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "echo ok"]
      interval: 30s
      timeout: 3s
      retries: 3
