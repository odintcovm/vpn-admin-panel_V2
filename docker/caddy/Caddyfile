:80 {
    encode gzip

    @api path /api/*
    handle @api {
        reverse_proxy api:8000
    }

    @sse path /api/events/stream*
    handle @sse {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy web:4173
    }
}
